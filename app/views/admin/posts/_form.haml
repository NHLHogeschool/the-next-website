= form_for [:admin, post], html: { class: 'post-form' } do |f|
  %p= f.text_field :title
  %p= f.text_area :body, rows: 15
  %p
    = f.submit
    Nog
    %span.character-count 140
    karakters over voor een automatische tweet.
